version: '3'

services:
  pytorch-ngc:
    container_name: inaki_pytorch
    image: nvdivia-pytorch-vitkd:v7
    build:
        context: .
        dockerfile: Dockerfile
    ipc: host
    tty: true
    ulimits:
      memlock: -1
      stack: 67108864
    ports:
      - '8888:8888'
      - '12355:12355'
    volumes:
      - '.:/app'
      - '/home-net/ierregue/research/data:/data'
    #command: bash -c "python train.py"
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [ 'gpu' ]